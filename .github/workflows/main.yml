#maestro: github actions
#a cada push na branch main, um robo aciona uma VM para executar estes comandos
#workflow customizado para segurança

name: DevSecOps

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
